CREATE TABLE SAMPLE_TABLE (
    ID INTEGER NOT NULL,
    NAME VARCHAR(256) NOT NULL
);

ALTER TABLE SAMPLE_TABLE ADD CONSTRAINT PK_SAMPLE_TABLE PRIMARY KEY (ID);

CREATE TABLE USERS (
    ID DM_ID NOT NULL,
    NAME DM_NAME,
    ADDRESS VARCHAR(200)
);


